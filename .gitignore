# Segredos e certificados reais - nunca versionar
secrets/*
!secrets/.gitkeep
certs/*
!certs/.gitkeep
.env
# Certificados do Traefik (Let's Encrypt, modo producao com
# docker-compose.prod.yml) vivem no volume nomeado "traefik-certificates",
# fora do repositorio - nao ha' arquivo local pra ignorar aqui.

# Backups locais
backups/
*.sql
*.sql.gz

# SO / editores
.DS_Store
Thumbs.db
